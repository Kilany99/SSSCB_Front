import { AccountRoutingModule } from './account-routing-module';

describe('AccountRoutingModule', () => {
  it('should create an instance', () => {
    expect(new AccountRoutingModule()).toBeTruthy();
  });
});
